# Local preview only. The published site is built by GitHub Pages, not from this file.
# The github-pages gem pins Jekyll and the default plugins to the exact versions
# GitHub Pages runs, so `bundle exec jekyll serve` matches production.

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
